tosca_definitions_version: tosca_simple_yaml_1_0


topology_template:

  policies:
    import-plugin:
      description: >-
        plugin to test import of plugin.yaml.
      type: aria.Plugin
      properties:
        version: 1.0.0

node_types:

  myapp.nodes.Network: {}
